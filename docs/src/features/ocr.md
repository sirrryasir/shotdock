## Instant Screen Text Extraction

1. Trigger OCR mode:
   ```sh
   shotdock -t
   ```
2. Drag over any text, code, or terminal output on screen.
3. The extracted text is instantly copied to your Wayland clipboard (`wl-copy`) and a notification toast displays a preview of the recognized text.
